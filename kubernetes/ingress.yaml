apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: react-modal
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /react-modal
            pathType: Prefix
            backend:
              service:
                name: react-modal
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
